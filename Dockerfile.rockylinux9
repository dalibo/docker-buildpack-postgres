FROM dalibo/buildpack-python:rockylinux9

RUN set -ex; \
    yum -qy install \
        postgresql18-server \
        postgresql16-server \
        postgresql14-server \
    ; \
    yum clean all ; \
    :